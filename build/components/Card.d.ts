declare type Props = {
    options?: {
        borderRadius?: boolean;
        showTags?: boolean;
        showDate?: boolean;
    };
};
declare function Card({ options }: Props): JSX.Element;
export default Card;
